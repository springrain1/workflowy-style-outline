# 更新日志

本项目的所有重要变更都将记录在此文件中。

---

## v3.1

### ✨ 新增功能

#### 极致性能与底层重构 (Performance & Architecture)
- **全局虚拟滚动架构**：为超大文档彻底重构了底层基座。引入全新的虚拟轮廓窗口 (`Virtual Outline Window`)，实现 DOM 节点的按需挂载与回收。从此告别万字长文、数千节点的卡顿问题，实现了真正的丝滑滚动。
- **全局渲染调度器**：引入了全新的帧调度器 (`Render Scheduler`)，将原本散落在各处的重排和 DOM 更新统一进行调度，彻底消除了高频编辑和快速滚动时的读写冲突与掉帧现象。
- **DOM 池化复用技术**：大纲的垂直缩进线现在会被智能放入对象池中复用。只需保留极其少量的元素，即可支撑无限长列表的渲染，大幅降低了内存占用。

#### 极致预览与原生体验 (Live Preview Evolution)
- **全面拥抱 CodeMirror 6 生态**：放弃了旧版的劫持方案，彻底重写了 Live Preview 引擎。现在，每个节点都由原生的 CodeMirror Widget 接管，像 Obsidian 核心编辑器一样底层解析 Markdown 语法并应用文本装饰 (Decorations)，渲染更加原生、精准。
- **全系悬浮菜单精准定位**：不仅是双链提示，包括 `/` 斜杠命令和 `#` 标签菜单，全部接入了全新的浮动定位逻辑。完美兼容虚拟列表的滚动偏移，确保在极速输入或视图滚动时，菜单均能实现与光标的像素级精准跟随，彻底消除闪烁或抖动。
- **无缝链接交互优化**：极大地强化了 Live Preview 下的链接操作体验。为 `[[` 链接建议引入防抖保护，不仅大幅提升了弹出响应速度，更彻底修复了偶尔无法用鼠标点击选中的顽疾。

#### 移动端与交互打磨 (Mobile & Interaction)
- **虚拟长列表跨屏拖拽多选**：打破了虚拟列表与鼠标操作的天然冲突！现在，即使拖拽跨越了屏幕外数百个未渲染的节点，系统也能精准计算出虚拟范围并完成多选操作，并在拖拽完成后自动清理多选状态。
- **移动端增量更新强化**：大幅增强了移动端的 `MobileDOMPatcher` 机制，更加优雅地处理跨层级节点的插入、删除与同步，从根本上根治了移动端虚拟列表的节点错乱问题。

### 🐛 修复
- **视图高度精确测量**：修复了 Daily Notes 聚合视图中，因虚拟高度测量不准确导致的视图意外跳动问题，并在文件被外部修改时能正确重载状态。
- **消除空状态无效渲染**：拦截了当文本内容未发生实质改变时仍然触发的无效状态更新，进一步榨干性能损耗。

---

## v3.0

### ✨ 新增功能

#### 界面与体验 (UI & UX)
- **原生“可读行宽”支持**：完美适配 Obsidian 核心的“可读行宽 (Readable Line Length)”设置。在大纲视图和日记聚合视图中，编辑器内容会自动居中并限制宽度，同时保持背景或分割线全宽显示，提供最舒适的阅读体验。
- **面包屑 Markdown 悬浮提示**：当鼠标悬停在面包屑导航的路径上时，现在会通过 Tooltip 渲染并显示该节点的完整 Markdown 内容。
- **右键菜单“插入双链”**：在 Live Preview 模式下新增“插入双链”快捷操作。选中文字时会自动用 `[[]]` 包裹并将光标置于末尾；未选中时会插入空链接并将光标置于括号内。

#### 交互增强 (Interactions)
- **精准光标恢复的键盘缩放**：使用快捷键缩放节点时，引入了光标历史栈机制。当您使用键盘缩放进入某个子节点编辑后，再退出缩放，光标会**分毫不差地恢复到您原来的具体字符位置**，而不是只能回到节点末尾。
- **Live Preview 鼠标多选革新**：彻底解决了输入框内文本选取与节点多选的冲突问题。您现在可以在节点内正常拖拽选取文本；只有当鼠标拖拽**越过当前节点边界**，或按住 `Alt` 键拖拽时，才会无缝升级为区块多选。同时在节点左侧新增了不可见的“多选导轨 (Selection Rail)”，方便直接发起多选。
- **方向键导航智能化**：重构了上下方向键的导航逻辑，现在基于真实 DOM 渲染树进行计算。完美跳过被过滤、隐藏或折叠的节点，再也不会出现光标意外乱跳的问题。

### 🐛 修复
- **彻底解决 Live Preview 内存泄漏**：重构了编辑器组件的事件生命周期管理，修复了高频切换焦点或视图时，快捷键和右键菜单事件监听器叠加导致的内存泄漏和性能下降问题。

---

## v2.9

### ✨ 新增功能

#### 文件夹大纲视图 (Folder Outline View)
- **一键聚合文件夹**：现在您可以右键点击文件列表中的任意文件夹，选择“打开为文件夹大纲”，将整个文件夹下的笔记无缝聚合为一个连贯的 workflowy 风格大纲。
- **智能标签页管理**：自动识别并复用已打开的文件夹视图，避免重复开启多个相同的标签页，让您的工作区始终保持整洁。
- **动态标题同步**：标签页上的名称会智能跟随您所选的文件夹，实时自动更新。

#### 跨视图光标无缝同步 (Seamless Cursor Sync)
- **双向光标记忆**：引入底层的 SourceMap 映射算法，无论您在大纲视图还是 Markdown 源码视图中编辑，只需一键切换，光标都会**精准保持在您刚才编辑的字元位置**（完美兼容多层级列表与折行文本）！再也不用在长篇文档里辛苦寻找刚才的编辑点了。
- **极致丝滑的切换体验**：Markdown 视图的光标恢复改用底层原生的 `eState` (瞬态状态) 接管，移除了容易引发竞态和延迟的定时器逻辑，实现了“零延迟”的瞬间无缝切换体验。

### 🚀 体验与性能提升

#### ⚡ 切换性能飞跃 (Smoother Transitions)
- **告别切换卡顿**：重构了视图切换时的状态传递机制，将光标等临时数据强制约束为内存态，不再写入持久化的 `workspace.json`。彻底消除了高频切换视图时产生的无效后台写入动作，不仅大幅降低了设备的能耗，还显著提升了 Obsidian 的整体响应速度。

#### 🏗 大文档加载优化 (第一期)
- **性能基础大升级**：针对超大文档（如包含数千个节点的笔记）完成了深度性能优化设计。定位了首屏同步长任务与强制重排瓶颈，彻底重构了底层渲染骨架，为即将到来的“极速打开”与“瞬间折叠”特性铺平了道路。

---

## v2.8.2

### 🔧 重构与优化
- **国际化 (i18n) 全面重构**：在聚合视图（Daily Notes Section）等组件中全面应用 `t()` 函数替代硬编码字符串，实现全插件多语言支持。
- **修复主题翻译失效**：移除 `THEMES` 静态常量过早导出的问题，改用动态 `getThemes()` 调用，彻底解决 i18n 初始化前获取回退语言的 Bug，并新增配套的 Vitest 测试用例。
- **渲染与滚动定位优化**：在 Daily Notes Section 挂载时引入双层 `requestAnimationFrame` 机制，彻底解决复杂 DOM 操作后的滚动定位偏差和视口抖动问题。
- **底层数据健壮性提升**：增强了日记的日期处理与查找创建的严谨性；完善了对 YAML Frontmatter 解析的安全过滤逻辑，现已完美兼容带有 BOM 字符和前导空行的特殊格式。

---

## v2.8.1

### 🐛 修复
- **YAML Frontmatter 格式保护**：解析器增加底层拦截机制，完美保护笔记头部的 YAML 属性区域（`---`），彻底解决了在启用 Thino 兼容模式时，YAML 中的空格被意外转换为 Tab 从而导致报错的问题。
- **视图切换体验优化**：修复了在单文件的大纲视图中点击“打开为 Markdown”时，不必要地新建标签页的问题。现在它会在当前标签页原地无缝切换，仅在聚合大纲模式下保留新建标签页逻辑。

---

## v2.8

### ✨ 新增功能

#### Daily Notes Plus 聚合大纲视图
- **全新的 Daily Notes 聚合视图**:在单一视图中展示多个 Daily Notes 的大纲,统一编辑管理
- **三种笔记来源模式**:
  - Daily Notes 模式:自动读取 Obsidian 核心 Daily Notes 插件配置
  - 文件夹模式:选择任意文件夹聚合显示
  - 标签模式:按标签筛选笔记
- **灵活的筛选和排序**:
  - 排序方式:日期(正序/逆序)、创建时间、修改时间、文件名
  - 时间范围:本周/本月/本年/上周/上月/上年/本季度/上季度/自定义范围
  - Preset 预设:保存常用配置,快速切换
- **高效的编辑体验**:
  - 每个 Daily Note 作为独立 section,支持折叠/展开
  - 点击标题打开为独立大纲视图
  - 缩放导航(Zoom):点击圆点聚焦子内容,面包屑导航显示层级
  - 完整的大纲编辑功能:缩进、移动、折叠、删除、多选等
  - 跨 section 拖拽块(普通拖拽移动,Alt+拖拽创建块引用)
  - 支持所有编辑器快捷键(Tab/Enter/Ctrl+Z 等)
- **性能优化**:
  - 按需加载:首屏预加载可配置数量的 section,滚动时动态挂载/卸载
  - 离开视口延迟卸载,避免快速滚动时重复挂载
- **便捷功能**:
  - 自动创建今日笔记:工具栏快捷按钮 + 可选的启动时自动创建
  - 日期选择器:快速跳转到指定日期的 Daily Note
  - Fallback 配置:核心插件不可用时使用插件级配置(文件夹、日期格式、模板路径)
  - 主题切换:支持所有内置主题,与独立视图保持一致
  - 响应式设计:完美适配桌面和移动端

#### 链接导航增强
- 统一的 Workflowy 链接导航系统:
  - 内部链接 `[[笔记]]`:当前视图打开或分屏
  - 标题链接 `[[笔记#标题]]`:滚动并高亮目标标题
  - 块引用 `[[笔记#^blockid]]`:精确导航到目标块
  - 嵌入链接右上角跳转按钮:点击打开源文件
- Ctrl+Alt+点击或Shift+点击分屏打开
- Daily Notes 聚合视图中的链接跳转:
  - 文件在聚合范围内:自动展开对应 section 并导航
  - 文件不在范围内:在新标签页打开独立视图
- 跳转后高亮效果:目标内容闪烁黄色背景 2 秒,渐变消失

#### 跨文档拖拽优化
- Alt+拖拽创建块引用的流程优化:
  - 同文档源码模式:快速路径,避免触发文件事件
  - 跨文档或 Live Preview:异步更新源块 ID,等待元数据就绪
  - 自动生成符合 Obsidian 规范的块 ID(^block-id 格式)
  - 完成后更新目标位置的块引用
- 跨 section 多选拖拽支持:
  - 检测多选拖拽数据格式(`workflowy-multi-blocks`)
  - 支持跨 Daily Notes section 批量移动块
  - 保持原有的拖拽区域判断(before/after/child)

### 🐛 修复
- 修复多选拖拽在跨 section 场景下的兼容性问题
- 修复文件未加载完成时保存导致的潜在数据丢失风险
- 修复编辑器容器在某些场景下丢失的问题
- 修复 subpath 导航事件的 payload 解析兼容性

### 🔧 改进
- 设置项深度合并:兼容旧版本缺失的嵌套配置字段
- 编辑器容器缓存:避免重复 DOM 查询,提升渲染性能
- 跨文档事件优化:明确的事件 payload 结构,支持 targetLeaf 指定
- 调试日志系统:通过 `settings.isolation.debugMode` 开启详细日志
- 命令统一:Daily Notes 视图和独立视图共享编辑命令(缩进、移动、折叠等)

### 📝 授权系统
- Daily Notes Plus 功能需要授权(试用 7 天)
- 授权管理界面:输入授权码、查看状态、赞赏码/公众号二维码
- 试用期间显示剩余天数提示
- 授权过期后弹窗引导

---

## v2.7

### ✨ 新增功能

#### 表格渲染增强
- Live Preview 模式完整支持 Markdown 表格:
  - 表格边框、表头背景、斑马纹
  - 表格内文本对齐(左/中/右)
  - 鼠标悬停高亮行
- 所有主题的表格样式适配(14 个内置主题)
- 表格换行逻辑优化:表格内部行用单换行符连接,避免解析错误

---

## v2.6

### ✨ 新增功能

#### 移动端双模式工具栏
- 键盘弹出时显示编辑工具栏（可横向滑动），键盘收起时自动切换为功能工具栏
- 编辑工具栏新增：撤销/重做、插入内部链接 `[[]]`、嵌入内容 `![[]]`、添加标签 `#`、插入附件（调用系统文件选择器）、插入链接 `[]()`
- 功能工具栏：切换渲染模式、展开所有块、折叠所有块、按级别展开（1级/2级/3级）
- 滑动防误触：手指移动超过 8px 阈值视为滑动，不触发按钮操作
- 收起键盘按钮固定在编辑工具栏最右侧

#### 移动端输入法闪烁修复（MobileDOMPatcher）
- 新增 `MobileDOMPatcher` 局部 DOM 更新器，避免全量重渲染导致键盘闪烁
- 工具栏缩进/取消缩进、新建块、切换待办状态均走局部更新路径
- 物理键盘 Enter/Backspace 在移动端走局部 DOM 更新路径
- Live Preview 模式 blur 延迟判定，避免工具栏操作时输入法收起再展开
- 编辑中的 textarea 在移动端始终复用，避免 detach 导致键盘闪烁

#### 移动端 UI 优化
- 内容行改为顶部对齐（`flex-start`），多行内容时圆点/checkbox 与第一行对齐
- Bullet 和 checkbox 添加 `margin-top: 2px` 精确对齐
- 新增有序列表序号顶部对齐样式
- 折叠指示器和复选框添加 `pointerdown` 防失焦处理
- 聚焦时移动端使用 `scrollIntoView({ block: 'center' })` 居中显示

### 🐛 修复
- 修复 Live Preview 模式下 Backspace 在光标开头时无响应的问题
- 修复移动端点击复选框/折叠指示器时焦点从编辑元素转移导致输入法闪烁
- 修复 `enterEditMode` 重入导致的重复执行问题
- 修复 `focus()` 方法在 Live Preview 模式下未正确激活编辑状态
- 修复移动端缩进、撤销操作可能导致上方节点内容丢失的数据同步问题
- 修复高频编辑时光标乱跳及 DOM 节点残留导致的内存泄漏问题

### 🔧 改进

#### 撤销/重做架构重构
- 重构为基于 Transaction 的日志撤销历史记录系统，支持多步骤合并操作的撤销与重做（Req 1.6）
- 引入三级 DOM 同步防闪烁策略：首先尝试 Content-only fast-path，其次是移动端增量 Patch，最后才回退到全量重新渲染，兼得性能与稳定性

#### 文件保存与并发控制
- 引入 `saveQueue` 队列和防抖任务 ID 控制写入顺序，从根本上解决高频输入和批量块操作下潜在的写入竞态与脏数据问题
- 升级 `saveAllEditingContent` 同步策略，不再依靠 DOM 搜索而是直接同步现有 OutlineItem 实例，提升了跨视图删除/更新时的鲁棒性

#### 移动端更新补偿（MobileDOMPatcher）
- `MobileDOMPatcher` 现在支持有序列表层级的同步调整（Sync Ordered Sibling Indices），避免前置插入导致排序号卡住
- 基于状态树精确匹配，防止退格删除唯一子节点或跨节点结构重绑带来的上下文丢失

## v2.5

### 🐛 修复
- 源码模式和Live Preview模式的多行内容↑/↓上下光标跳转至上下节点的问题解决了，实现了上下行间跳转。

## v2.4

### ✨ 新增功能

#### UI 与交互增强
- 新增部分设置选项（字体大小、行高、圆点对齐、字体来源等）
- 支持 `Ctrl+滚轮` 快速调整字体大小（10-30px）
- 圆点大小跟随字体大小自动调整
- 圆点垂直中心始终与第一行文本的垂直中心对齐
- 字体大小修改后，Live Preview 模式渲染层保持与编辑层一致
- 折叠指示器修改为 SVG 方案

#### Thino 兼容模式
- 新增 Thino/Memos 插件兼容模式
- **启用 Thino 兼容模式后**：
  - 第 1 级列表项的续行内容保留原始缩进格式（Tab 或空格）
  - 第 2~n 级列表项的续行保持标准两个空格格式
  - Markdown ↔ 大纲视图切换时保持原始缩进不变
  - 新建节点自动添加时间戳功能（可选，支持 `HH:mm` 或 `HH:mm:ss`）
  - 新增快捷键命令：`Workflowy: 切换新建节点自动添加时间戳`
- **关闭 Thino 兼容模式时**：
  - 自动将 Tab 转为空格，续行统一为两个空格格式

### 🐛 修复
- 实时预览模式下单节点续行内容的有序/无序列表渲染层显示问题
- 续行内容字符串两端的空白字符未正常保留
- Thino 兼容开关未生效的问题
- 续行检测的正则表达式将以数字开头的行误判为列表项

### 🔧 改进

#### 联动与兼容性
- 优化与 Thino、TickTickSync 插件的兼容性
- 改进续行缩进类型的检测与还原机制

#### 内存管理优化
- 新增组件销毁机制，防止内存泄漏
- 为 `ZoomManager`、`ThemeManager`、`FileDropHandler` 添加 `destroy()` 方法
- 增强 `TagSuggestMenu`、`SlashCommandMenu` 的资源清理
- 新增 `OutlineItem.cleanupStaticResources()` 静态方法，清理拖拽相关资源

#### 生命周期管理
- 增强 `WorkflowyView.onClose()` 级联销毁所有子组件
- 添加 `isClosing` 标志防止视图关闭后继续异步操作
- 添加 `wheelHandler` 事件追踪，确保正确移除事件监听器

#### 防御性编程
- 为 `VerticalLinesManager`、`MultiSelectionManager` 添加 `isDestroyed` 检查
- 在异步操作完成后检查组件状态，避免更新已销毁视图
- 增强插件 `onunload()` 清理静态资源

---

## v2.3

### ✨ 新增功能
- `/` 打开斜杠命令菜单，迁移 obsidian-tasks 插件编辑任务功能（兼容 TickTickSync）

| 命令类型 | 功能 |
|----------|------|
| **时间** | 设置任务时间 `[HH:MM]`，支持时间范围 |
| **优先级** | 🔺最高 / ⏫高 / 🔼中 / 🔽低 / ⏬最低 |
| **日期** | 📅截止日期 / ⏳计划日期 / 🛫开始日期 / ➕创建日期 / ✅完成日期 |
| **状态** | ☐待办 / ◐进行中 / ☑已完成 / ⊘已取消 / •普通项目 |

- 添加时间选择器、日期选择器
- 分组显示斜杠命令，带有分组标题
- 使用 title 属性为斜杠命令菜单添加中文悬浮提示
- 命令重复输入时自动替换而非追加
- `#` 打开标签建议菜单，实时过滤并快速插入
- 菜单键盘导航支持：↑/↓ 导航，Enter 选择，Escape 关闭

### 🐛 修复
- `/` 命令导致的光标位置问题
- 菜单键盘导航失效问题
- 状态命令修改底层 Markdown 时触发的 `Uncaught TypeError` 错误
- 节点换行中的空行在切换回 Markdown 视图时丢失的问题
- Live Preview 模式渲染层 Markdown 元素（标题、代码块等）的换行逻辑优化
- 斜杠命令和标签建议菜单未跟随输入光标位置的问题

---

## v2.2

### 🔧 改进
- 有序列表自动排序
- 大纲视图下编辑的有序列表与 Markdown 视图双向同步

### 🐛 修复
- 有序列表自动被修改为无序列表的问题
- 有序列表与节点内容间距过大的问题
- `Ctrl+Z` 或跨文档拖拽导致的有序列表序号丢失问题

---

## v2.1

### ✨ 新增功能
- 移动端工具栏按钮：待办、加粗、斜体、删除线、高亮、代码、缩进控制、回车、收键盘
- 重新设计移动端折叠指示器（放至右侧，展开 ▼，折叠 ◀）

### 🐛 修复
- 移动端圆圈节点样式过大的问题（改用 `::after` 伪元素）
- 移动端节点折叠后中心显示矩形方框的问题
- 移动端垂直线未对齐圆圈中心的问题
- 三角形折叠指示器位置错位问题

---

## v2.0

### ✨ 新增功能
- 类 Logseq 快捷键及功能：`Shift+点击`、`Ctrl+悬浮`、`Alt+创建块引用`
- 块跨文档单选与多选拖拽功能（通过节点圆圈）
- 跨/本文档 `Alt+拖拽` 自动创建块引用
- 前缀为 `!` 的标题/块引用双链跳转及高亮显示

### 🐛 修复
- 删除当前块（`Ctrl+Shift+Backspace`）后的光标定位问题

---

## v1.9

### ✨ 新增功能
- 支持从剪贴板粘贴图片或从文件列表拖拽图片至视图
- 支持拖拽文件生成链接（.md 为双链，其他为嵌入链接）
- Live Preview 及源码模式拖拽文件增加光标指示
- 标题引用、块引用的双链跳转及高亮效果

### 🐛 修复
- 将"删除当前块"、"向上/下移动块"快捷键改为内置
- 补充快捷键：清空当前节点内容（`Ctrl+Backspace`）

---

## v1.8

### ✨ 新增功能
- 保存节点折叠/展开状态至 Markdown（使用 `<!--c-->` 标记）
- Live Preview 模式编辑层隐藏块引用 ID，防止误编辑

### 🐛 修复
- 块引用 ID 与折叠标记冲突的问题
- Live Preview 块引用链接格式修正（补全 `#` 后的空格）
- `Shift+Enter` 换行内容导致折叠标记解析错误的问题

---

## v1.7

### ✨ 新增功能
- 为空 Workflowy 视图添加新增节点提示
- 支持多分屏下同文件的双向实时更新（兼容 Mindmap NextGen）
- 增加切换渲染模式（源码/Live Preview）的快捷命令

### 🐛 修复
- Live Preview 编辑模式下方向键导航失效的问题
- 移除冗余的快捷键命令配置，减少冲突

---

## v1.6

### 🔧 改进
- 异步嵌入内容（图片、Dataview、Excalidraw 等）复用 DOM 元素，避免闪烁
- 嵌入内容增加跳转图标按钮，支持 Excalidraw 图片双击
- 使用 `AbstractInputSuggest` 实现双链、标签及块引用的输入建议
- 优化拖拽逻辑，区分文本选择与块拖拽

### 🐛 修复
- 嵌入内容导致的后续节点垂直线渲染错误
- 代码块缩进丢失问题
- 窗口缩放或边栏变动时自动重绘垂直线

---

## v1.5

### 🐛 修复
- 标签点击搜索及图片渲染问题
- 嵌入内容点击跳转及图标显示
- 嵌入内容适配各类主题样式

---

## v1.4

### 🐛 修复
- 子节点缩进随动逻辑
- 多选节点缩进逻辑
- 缩进（Tab/Shift+Tab）逻辑错误
- 源码/Live Preview 模式下 Enter 键导致的闪烁问题
- 限制显示层拖拽选取多选节点，避免编辑层干扰
- 编辑节点时光标/视口跳动问题
- 操作节点时的 Console 报错
- 优化撤销（Undo）底线，避免进入空状态

---

## v1.3

### 🐛 修复
- Live Preview 模式下 Backspace 无法删除空节点的问题
- 拖拽节点后垂直线渲染不正确的问题
- 内存泄漏及性能优化

---

## v1.2

### ✨ 新增功能
- 实时预览模式（Live Preview，类似 Logseq）
- 渲染模式配置选项（源码模式 / 实时预览模式）

---

## v1.1

### ✨ 新增功能
- 支持非列表项（非 `- ` 开头）的只读 Markdown 渲染
- 支持双链面板跳转及前进/返回导航

---

## v1.0

### 🎉 首次发布

**大纲视图功能：**
- 顶部导航栏，支持节点导航和搜索（12 款内置主题）
- 4 个空格为一个 Tab 缩进，`- ` 开头格式（类似 Logseq）

**点击操作：**
- 点击左侧垂直线可折叠/展开节点
- 点击圆圈进入缩放聚焦模式
- 按住鼠标左键上下拖拽多选节点，Ctrl+点击选择不连续节点

**快捷键：**
- `Enter` - 创建新节点
- `Tab` / `Shift+Tab` - 增加/减少缩进
- `Ctrl+Enter` - 切换待办状态（再按一次完成，子节点变灰）
- `Ctrl+Shift+↑/↓` - 上下移动节点
- `Alt+↑/↓` - 缩放导航
- `Shift+Enter` - 节点内换行
- `Ctrl+Backspace` - 清空节点内容
- `Ctrl+Shift+Backspace` - 删除当前节点
